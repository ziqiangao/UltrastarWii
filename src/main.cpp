#include <mad.h>
#include <ogc/audio.h>
#include <gccore.h>
#include <fat.h>

struct mad_stream stream; // holds input MP3 data
struct mad_frame frame;   // holds decoded frame info
struct mad_synth synth;   // synthesizes PCM from decoded frame

int main()
{
    mad_decoder_init()

    return 0;
}